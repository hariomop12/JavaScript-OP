const course = {
  course: "JavaScript",
  lessons: 40,
  price: 3000,
};

const { lessons, price } = course;
// destructuring the object is a way to extract the values from the object and store them in variables
// eg - const {lessons, price} = course;
// this will extract the lessons and price from the course object and store them in lessons and price variables respectively
console.log(lessons, price); // 40 3000

// object with out name is JSON
// {

// }

const OP = {
  course: "JavaScript",
  lessons: "40",
  price: "3000",
}

[({}, {}, {})];
